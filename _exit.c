#include "holberton.h"

/**
 * exit_process - exit all process
 * @nb: status number
 * Return: nothing
 */
int exit_process(int nb, char **s, envNodes **env)
{
//	int status = 0;
//	char const *tmp;
	(void)nb;
	(void)s;
	(void)env;
//	tmp = env[1];
	//status = atoi(tmp);
	exit(98);
}
